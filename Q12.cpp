/*check whether given char is  consonant or not */
#include <iostream>
using namespace std;
int main()
{
    char letter;
    cout<<"Enter Character = ";
    cin>>letter;
    if(letter=='a' || letter=='e' ||letter=='i' || letter=='o' || letter=='u')
    {
        cout<<"Given letter is not a consonant";

    }
    else
    {
        cout<<"Given letter is not a consonant";
    }
    return 0;
}
